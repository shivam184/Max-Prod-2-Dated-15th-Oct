@isTest
public class LocationCoordinateTest {
	
    @isTest
    static void testMe(){        
        Test.startTest();
        LocationCoordinate l = new LocationCoordinate(20, 30);
        Test.stopTest();
    }
}